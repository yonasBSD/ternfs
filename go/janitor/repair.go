package janitor

/*
import "xtx/eggsfs/msgs"

// Migrates the block at that offset, and that block service, to a new block service.
func (env *Env) MigrateBlock(fileId msgs.InodeId, byteOffset uint64, ip [4]byte, port uint16) error {
	req := msgs.FileSpansReq{
		FileId:     fileId,
		ByteOffset: byteOffset,
	}
	resp := msgs.FileSpansResp{}
	err := env.ShardRequest(&req, &resp)
	if err != nil {
		return err
	}
}
*/
